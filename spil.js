var canvasW = 1000
var canvasH = 500
var side = 0



function setup() {
    createCanvas(canvasW, canvasH);
    background(153);

  }


function draw () {
    if (side === 2) {
        background(0);
     }
}